# Directives — {{WORKSPACE_NAME}}

This workspace's own standing rules: identity, scope, counterparties, procedures — everything
that is true of *this* workspace and of no other.

**The plugin never writes this file.** A plugin update may replace `AGENTS.md` (the standard
router) wholesale; what lives here survives every update. On conflict with `AGENTS.md`, this
file wins.

Keep it in the router's spirit: short sections, link out to `.joserah/` for detail, and prune
what stops being true.

## Identity

<!-- Who the assistant is in this workspace, if it differs from the default.
     Name, voice, signature, who it serves. -->

## Scope

<!-- What this workspace covers, and — just as important — what it must refuse.
     Hard boundaries go here, stated so a cold session cannot miss them. -->

## Procedures

<!-- The workflows that repeat: how a round of work runs, what gets sent where,
     what is verified before anything ships. -->
