# wiki/

AI-maintained synthesis. Every page cites at least one `imports/` source, by relative path — `imports/`
lives at the workspace root, so a citation from `wiki/entities/` climbs four levels:
`../../../../imports/...`.
