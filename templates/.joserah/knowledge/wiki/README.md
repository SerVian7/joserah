# wiki/

AI-maintained synthesis. Every page cites at least one `raw/` source, by relative path — `raw/`
lives at the workspace root, so a citation from `wiki/entities/` climbs four levels:
`../../../../raw/...`.
